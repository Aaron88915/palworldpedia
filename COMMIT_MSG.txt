fix: active filter chip white background bug

Bug: Astro scoped CSS .active-chip[data-astro-cid-xxx] didn't match
JS-generated elements (created via innerHTML), so the chip fell back
to default browser button styling (white bg, ugly in dark theme).

Fix: move .active-chip and .reset-link styles to global.css so they
apply everywhere. Also redesign active chip to subtle outlined style
(dark bg + accent border + accent text + orange fill on hover) instead
of solid orange - matches dark theme better.

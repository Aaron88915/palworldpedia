fix: remove 153 empty <a> tags in guide pages (no href)

Earlier fix scripts replaced href="X" with empty string when the
target pal/tech was unknown, but left the <a> tag wrapper intact.
This created <a>text</a> shells with no href, which still got
the accent color + underline styling but were not clickable.

User report: "some underline links can't be clicked, cursor doesn't
change to pointer hand on hover".

Fixed:
- 77 empty <a> in 5 ZH guides
- 76 empty <a> in 5 EN guides

Affected text (now plain text, was fake links):
- Berry Plantation, Ranch, Pal Gear Workbench, Electric Fence
- 玩家床, 帕鲁床, 火把
- Caprity, Petallia, Cattiva, Lifmunk, Foxparks (these names
  appear multiple times in different contexts)

New tool: scripts/fix-empty-a-tags.py removes all <a> tags
without href across the site.
Verify: scripts/verify-no-empty-a.py reports 0 across 1784 pages.

Build: 1784 pages, 0 errors.

fix: rename AdSlot `slot` prop to `position` to fix inline ad rendering

Bug: AdSlot 用 `slot` 作 prop 名跟 Astro 内部 slot 机制冲突，
导致 `<AdSlot slot="inline" />` 在页面顶层被 Astro 静默丢弃
（top/bottom 恰好能渲染，但 591 页的 inline 全部消失）。

Fix: 把 prop 改名为 `position`，所有调用点同步更新。

变更：
- src/components/AdSlot.astro: prop slot → position
- src/layouts/Layout.astro: 2 处
- src/pages/*.astro: 14 处

验证：885 页 = top+inline+bottom=3，1 页（404 showTopAd=false）= 2
Build 通过：886 页 / 9.7s
